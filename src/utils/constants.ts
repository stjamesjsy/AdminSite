import config from "../../config.json";

export const CDN_URL = config.cdnUrl;
export const API_KEY = config.apiKey;
export const ADMIN_API_KEY = config.adminApiKey;
export const API_URL = config.apiUrl;
export const GCP_BUCKET_NAME = config.bucketName;